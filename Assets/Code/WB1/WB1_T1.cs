using UnityEngine;

public class WB1_T1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello, Unity!"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
